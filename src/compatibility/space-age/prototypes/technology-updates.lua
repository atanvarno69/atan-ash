if not mods["space-age"] then
    return
end

local util = require("functions")

util.add_recipe_unlock("biochamber", "atan-nutrients-from-ash")
util.add_recipe_unlock("foundation", "atan-foundation-from-ash")
